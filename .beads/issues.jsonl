{"id":"loupe-1mf","title":"Persist review state and harden the agent pipeline","description":"Umbrella for the next significant changes to loupe after the initial scaffold. The MVP keeps comment submit/resolve state in frontend memory only and has a known agent-console glitch. This epic tracks making review state durable and the agent stream reliable, and is the parent for follow-on work in that area.","acceptance_criteria":"Review/comment state survives a page reload; the agent console renders each message exactly once; child issues completed.","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-05-20T15:57:08.309858-04:00","created_by":"smore","updated_at":"2026-05-20T16:30:05.127822-04:00","closed_at":"2026-05-20T16:30:05.127822-04:00","close_reason":"Superseded: pivoting loupe from a Go web app to a VS Code extension (scorched-earth restart)."}
{"id":"loupe-1mf.1","title":"Persist comment submit/resolve state across reloads","description":"Comment submitted/collapsed/edit state currently lives only in React state (web/src/App.tsx), so a page refresh loses which comments were submitted/resolved. The backend already stores submitted comment rows per review but the frontend never reloads them.\n\nScope:\n- Add a 'resolved' (and/or 'submitted') column to the comments table in internal/store/store.go.\n- Add GET endpoints to list reviews and their comments in internal/server/server.go.\n- On load, hydrate the frontend comment list (with ids, submitted, collapsed) from the backend instead of starting empty.\n- Persist edits/deletes/resolve-toggles back to the server.","acceptance_criteria":"After submitting comments and reloading the page, submitted comments reappear collapsed/resolved with correct anchors; pending count and submit gating are preserved; edits and deletes persist.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-05-20T15:57:19.720518-04:00","created_by":"smore","updated_at":"2026-05-20T16:30:05.132785-04:00","closed_at":"2026-05-20T16:30:05.132785-04:00","close_reason":"Superseded: pivoting loupe from a Go web app to a VS Code extension (scorched-earth restart).","dependencies":[{"issue_id":"loupe-1mf.1","depends_on_id":"loupe-1mf","type":"parent-child","created_at":"2026-05-20T15:57:19.724193-04:00","created_by":"smore"}],"comments":[{"id":1,"issue_id":"loupe-1mf.1","author":"coordinator","text":"Implementation complete: Implemented loupe-1mf.1: persist comment submit/resolve state across reloads.\n\nSTORE (internal/store/store.go): added `submitted`/`collapsed` columns to the comments table with idempotent ALTER migrations (handles the pre-existing .loupe/loupe.db); AddComment now returns the new id; added EnsureReview (get-or-create one long-lived review per branch/base pair), Reviews(branch,base) listing, UpdateComment, CommentByID, DeleteComment, MarkSubmitted (flags pending→submitted+collapsed, returns the batch), SetMode, and a scanComments helper.\n\nSERVER (internal/server/server.go): new routes — GET /api/reviews (filter by ?branch\u0026base, comments embedded for one-call hydration), POST /api/reviews (ensure draft, no agent run), GET/POST /api/reviews/{id}/comments, PATCH /api/comments/{id} (partial update via pointer fields), DELETE /api/comments/{id}, POST /api/reviews/{id}/submit (marks the pending batch submitted, then runs the agent on just that batch). runReview refactored to take the submitted batch. WebSocket route unchanged.\n\nFRONTEND (web/src/api.ts, web/src/App.tsx): comment ids are now backend integers; on branch/base load the app calls ensureReview + listReviews and hydrates comments with their submitted/collapsed flags instead of starting empty; create/edit/delete/collapse-toggle/submit all persist to the server.\n\nQuality gates: `go build ./...` clean, `go vet ./...` clean, `cd web \u0026\u0026 npm install \u0026\u0026 npm run build` succeeds (binary re-embeds web_dist via make build).\n\nRuntime smoke test (API-level, exactly what a browser reload exercises): created review → added comment → submitted → re-fetched: comment reappears with submitted:true, collapsed:true and correct path/line/side anchor. Edits persist (reopen sets submitted:false, body updated). Deletes persist (count 2→1). Empty-pending submit is gated with 400. Submit correctly spawned the claude agent (and used --resume with the stored session_id on the second run). DB restored to its pre-test state afterward; no stray processes left.\n\nAcceptance: 4/4 criteria met. NOT committed — awaiting review approval. Files changed: internal/store/store.go, internal/server/server.go, web/src/api.ts, web/src/App.tsx, internal/server/web_dist/index.html (rebuilt).","created_at":"2026-05-20T20:17:49Z"},{"id":2,"issue_id":"loupe-1mf.1","author":"coordinator","text":"Review APPROVED by worker-2","created_at":"2026-05-20T20:20:10Z"},{"id":3,"issue_id":"loupe-1mf.1","author":"coordinator","text":"Task completed","created_at":"2026-05-20T20:21:51Z"}]}
{"id":"loupe-1mf.2","title":"Fix duplicate final message in agent console","description":"The agent's closing summary renders twice in the browser console. The agent emits its final text as an assistant 'text' event, and the terminal 'result' event carries the same text again; both are pushed to the events list in web/src/App.tsx.\n\nFix options:\n- Stop forwarding result text as displayable content in internal/agent/agent.go (use EventResult only as a done marker), or\n- In the frontend, skip rendering a 'result' event whose text duplicates the previous 'text' event.","acceptance_criteria":"A completed agent run shows its final summary exactly once in the console.","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-05-20T15:57:20.028016-04:00","created_by":"smore","updated_at":"2026-05-20T16:30:05.134302-04:00","closed_at":"2026-05-20T16:30:05.134302-04:00","close_reason":"Superseded: pivoting loupe from a Go web app to a VS Code extension (scorched-earth restart).","dependencies":[{"issue_id":"loupe-1mf.2","depends_on_id":"loupe-1mf","type":"parent-child","created_at":"2026-05-20T15:57:20.028743-04:00","created_by":"smore"}],"comments":[{"id":4,"issue_id":"loupe-1mf.2","author":"coordinator","text":"Implementation complete: Fixed duplicate final summary in the agent console (loupe-1mf.2).\n\nROOT CAUSE: claude's stream-json emits the final assistant prose as an `assistant`/`text` line AND repeats it in the terminal `result` line. agent.go forwards both as EventText (agent.go:97) and EventResult (agent.go:104), and the frontend rendered both → summary shown twice.\n\nFIX (frontend layer, web/src/App.tsx ws.onmessage): the `result` event is now rendered only when its trimmed text differs from the preceding event's text (and is non-empty); otherwise it's skipped as a display item while `setRunning(false)` still fires as the done-marker. Chose the frontend over agent.go because the `result` field is sometimes the SOLE carrier of final text (error subtypes, runs ending on tool_use) — dropping it at the source would lose the summary in those cases. This guarantees exactly-once in both the normal (duplicate) and edge (sole-carrier) cases.\n\nVERIFICATION:\n- Captured a REAL `claude -p --output-format stream-json` run: confirmed assistant text and result.result are byte-identical (\"The summary is done.\").\n- Replayed that real stream through agent.go's exact transform + the new frontend reducer: summary renders exactly 1 time, running=false fires. PASS.\n- Edge cases (sole-carrier, differing result text, empty result) all verified: summary never dropped, no empty noise.\n- No regression to loupe-1mf.1 comment hydration (untouched useEffect).\n\nQUALITY GATES: `go build ./...` clean; `go vet ./...` clean; `cd web \u0026\u0026 npm install \u0026\u0026 npm run build` succeeds; `make build \u0026\u0026 ./loupe` boots and serves the SPA (verified /api/branches + SPA title).\n\nFiles changed: web/src/App.tsx (1 handler; +11/-1). NOT committed — awaiting review approval.","created_at":"2026-05-20T20:25:39Z"}]}
{"id":"loupe-2sq","title":"Changeset navigation \u0026 uncommitted-change review","description":"Two related improvements to how loupe presents and navigates a changeset:\n\n1. Review uncommitted/working-tree changes (not just committed branch diffs).\n2. A collapsible file-tree sidebar on the left for navigating files in a changeset.\n\nBoth are mostly frontend with a thin backend slice for the working-tree diff. They share the diff-rendering and review-state code paths in web/src/App.tsx and internal/git. See child issues for detail.","design":"Current state (researched 2026-05-20):\n- git.Diff runs 'git diff \u003cmerge-base(base,branch)\u003e \u003cbranch\u003e' — commit-to-commit only (internal/git/git.go:63).\n- Comments anchor on path+line only; store.Comment.BlobSHA exists but the frontend never sends it (CommentInput is {path,side,line,body}), so blob_sha is always empty.\n- Reviews are keyed by (branch, base) in EnsureReview (store.go:96); that key flows into persistence, the agent prompt, and the WebSocket.\n- Direct mode already edits the working tree (adapter.go:45).\n- Layout is a 2-col grid: .layout { grid-template-columns: 1fr 360px } (diff + right sidebar, styles.css:44). File \u003csection\u003es have no scroll-target id yet. files: FileDiff[] (each with .path) is already in App state.\n- Vitest + go test harness now exist (make test) — new pure modules and git_test.go are testable.","status":"closed","priority":2,"issue_type":"epic","created_at":"2026-05-20T21:23:21.283703-04:00","created_by":"smore","updated_at":"2026-05-20T23:12:31.377155-04:00","closed_at":"2026-05-20T23:12:31.377155-04:00","close_reason":"All tasks completed, branch merged to main locally (73b9069)"}
{"id":"loupe-2sq.1","title":"Review uncommitted/working-tree changes","description":"Let loupe review the working tree (committed + uncommitted changes), not just committed branch diffs. Treat the working tree as a selectable pseudo-branch via a sentinel value (e.g. git.WorkingRef = '*working*') so it reuses the existing base/branch UI, (branch,base) persistence key, prompt path, and WebSocket with minimal new surface.\n\n## Backend\n1. internal/git/git.go — add DiffWorking(base string) (string, error): mb := merge-base(base, HEAD); 'git diff --no-color \u003cmb\u003e' (working tree vs base — committed + uncommitted together; subsumes 'git diff HEAD' when base is the current branch tip, so uncommitted edits on main work too). Untracked files do NOT appear in plain 'git diff' — either enumerate 'git ls-files --others --exclude-standard' and 'git diff --no-index /dev/null \u003cfile\u003e' per file, or document untracked as out of scope for v1.\n2. internal/server/server.go — define one shared sentinel const; in handleDiff, when branch == sentinel call DiffWorking(base); apply the same dispatch in runReview (server.go:248) where it currently calls s.git.Diff.\n3. internal/adapter/adapter.go — special-case the prompt header wording when branch is the sentinel ('reviewing uncommitted changes against base').\n\n## Frontend\n4. web/src/App.tsx — add a 'Working tree (uncommitted)' option at the top of the branch dropdown mapping to the sentinel. No API shape changes; ensureReview/submitReview/persistence key off the sentinel string transparently.\n5. Add a 'Refresh diff' button — working-tree diffs are live, but the current effect only refetches on base/branch change.\n\n## Trade-offs\n- Anchor drift: as you edit, line numbers shift and stored comments can land on the wrong line. Acceptable for v1 (same model as today); future fix is content-based anchoring instead of raw line number.\n- Sentinel collision with a real branch literally named '*working*' is vanishingly unlikely; alternative is a dedicated ?working=1 param, but persistence still needs a synthetic branch key, so the sentinel is less code.","acceptance_criteria":"Selecting 'Working tree (uncommitted)' as the branch shows a diff of committed + uncommitted changes vs the base; comments can be added, persist, and submit to the agent on that view; runReview uses the working-tree diff for the prompt; a Refresh button re-pulls the live diff. internal/git/git_test.go covers DiffWorking (base commit + committed change + uncommitted edit + untracked file) and a server test confirms the sentinel routes to the working-tree diff. 'go test ./...' passes.","notes":"Resolves the question from the prior session: loupe currently only diffs committed state.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-05-20T21:23:41.975214-04:00","created_by":"smore","updated_at":"2026-05-20T22:23:26.452389-04:00","closed_at":"2026-05-20T22:23:26.452393-04:00","dependencies":[{"issue_id":"loupe-2sq.1","depends_on_id":"loupe-2sq","type":"parent-child","created_at":"2026-05-20T21:23:41.9782-04:00","created_by":"smore"}],"comments":[{"id":5,"issue_id":"loupe-2sq.1","author":"coordinator","text":"Implementation complete: Implemented \"Review uncommitted/working-tree changes\" (loupe-2sq.1).\n\nBACKEND:\n- internal/git/git.go: DiffWorking(base) diffs working tree (committed+uncommitted) vs merge-base(base,HEAD) and appends untracked files via `git diff --no-index`; shared sentinel const git.WorkingRef = \"*working*\". (DiffWorking was partially present from the prior session; completed/verified.)\n- internal/server/server.go: added diffFor(base,branch) that dispatches the sentinel to DiffWorking; wired into BOTH handleDiff and runReview (so the live view and the agent prompt use the same diff).\n- internal/adapter/adapter.go: BuildPrompt special-cases the sentinel with \"reviewing uncommitted changes against base\" header wording.\n\nFRONTEND (web/src/App.tsx):\n- Added \"Working tree (uncommitted)\" option at the top of the branch dropdown mapping to the sentinel; ensureReview/submitReview/persistence key off the sentinel string with no API shape changes.\n- Added a \"Refresh diff\" button; split the diff fetch into its own effect (keyed on base/branch/refreshKey) so live working-tree diffs re-pull without resetting comment state.\n\nTESTS:\n- internal/git/git_test.go (new): DiffWorking covers base commit + committed change + uncommitted edit + untracked file, plus a test confirming it diverges from the committed-only Diff.\n- internal/server/server_test.go: TestDiffWorkingRefRoutesToWorkingTree spins up a real git repo and confirms /api/diff?branch=*working* surfaces an uncommitted edit the committed diff does not.\n\nVERIFICATION:\n- `make test` passes: go test ./... (git/server/store OK) + vitest 9/9.\n- Runtime smoke test (built binary on :7878 + agent-browser): selected \"Working tree (uncommitted)\" → 8 file sections rendered, console clean, no WebSocket errors; Refresh button works; added a comment that persisted under branch=\"*working*\" (verified via /api/reviews), then cleaned it up. Server/browser stopped.\n\nAcceptance: 7/7 criteria met. Note: pre-existing tsc error in web/src/highlight.ts (untouched, not in build/test path). NOT committed per instructions — awaiting your review/approval.\n\nFiles: internal/git/git.go, internal/git/git_test.go, internal/server/server.go, internal/server/server_test.go, internal/adapter/adapter.go, web/src/App.tsx (+ rebuilt internal/server/web_dist/index.html).","created_at":"2026-05-21T02:18:11Z"},{"id":6,"issue_id":"loupe-2sq.1","author":"coordinator","text":"Review APPROVED by worker-2","created_at":"2026-05-21T02:20:55Z"},{"id":7,"issue_id":"loupe-2sq.1","author":"coordinator","text":"Task completed","created_at":"2026-05-21T02:23:26Z"}]}
{"id":"loupe-2sq.2","title":"Collapsible file-tree sidebar (left) for changeset navigation","description":"Add a left panel that turns the flat file list into a navigable, collapsible tree; clicking a file scrolls the diff to it. The panel itself can also collapse to a thin rail.\n\n## New pure module — web/src/filetree.ts (unit-testable)\n- buildTree(paths: string[]): TreeNode[] — fold 'a/b/c.go' paths into nested dir/file nodes, dirs-first + alphabetical. This is the only piece with real logic; gets filetree.test.ts (Vitest harness already exists).\n\n## New component — web/src/FileTree.tsx\n- Recursive render; directory nodes collapsible (local useState\u003cSet\u003cstring\u003e\u003e of collapsed dirs, or native \u003cdetails\u003e); file nodes call onSelect(path).\n- Optional per-file badges: pending-comment count (from existing comments state — high value for review nav) and +/- line counts (computable from FileDiff.hunks).\n\n## App.tsx wiring\n- Give each file \u003csection\u003e a stable id (fileId(path)); add scrollToFile(path) reusing the existing jumpTo smooth-scroll + flash pattern.\n- Add sidebarCollapsed state + a toggle (header button or rail chevron). Pass files, comment counts, and active path to \u003cFileTree\u003e.\n- Optional scroll-spy (IntersectionObserver) to highlight the file currently in view.\n\n## styles.css\n- Change .layout to grid-template-columns: \u003ctree\u003e 1fr 360px; collapsed state shrinks the tree column to a narrow rail (~28px) with just an expand chevron.\n- Add .filetree panel, indentation, dir/file rows, hover/active highlight, long-name truncation — using existing --panel/--border/--accent/--muted tokens to match the theme.","acceptance_criteria":"A left sidebar shows the changeset as a collapsible directory tree; clicking a file scrolls the diff to that file's section (with the existing flash). Directories expand/collapse and the whole panel collapses to a rail. buildTree is covered by web/src/filetree.test.ts and 'make test' passes. Empty changeset, deep nesting, and many-file cases render sanely.","notes":"files: FileDiff[] is already in App state, so no new data fetching is needed.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-05-20T21:23:42.27566-04:00","created_by":"smore","updated_at":"2026-05-20T22:37:05.748265-04:00","closed_at":"2026-05-20T22:37:05.748269-04:00","dependencies":[{"issue_id":"loupe-2sq.2","depends_on_id":"loupe-2sq","type":"parent-child","created_at":"2026-05-20T21:23:42.276365-04:00","created_by":"smore"}],"comments":[{"id":8,"issue_id":"loupe-2sq.2","author":"coordinator","text":"Implementation complete: Implemented the collapsible file-tree sidebar (loupe-2sq.2). New pure module web/src/filetree.ts (buildTree folds paths into dirs-first/alphabetical TreeNodes) + web/src/filetree.test.ts (16 tests: empty changeset, blank/stray-slash paths, deep nesting, prefix merging, dirs-first ordering, duplicate collapse). New component web/src/FileTreePanel.tsx (recursive render, local useState\u003cSet\u003e collapsed dirs, file rows call onSelect, per-file 💬 pending-comment + ± line-count badges). App.tsx wiring: fileId(path) stable section ids + data-path, scrollToFile() reusing jumpTo flash pattern (added .file.flash keyframe), treeCollapsed rail toggle, fileMeta from FileDiff.hunks + pending comments, and an IntersectionObserver scroll-spy highlighting the active file. styles.css: .layout now 260px/1fr/360px grid (28px rail when collapsed) plus .filetree panel/rail/row/badge styles using existing theme tokens.\n\nTests: make test green (go ./... + 16 vitest, all pass). tsc: no NEW errors (only the pre-existing out-of-scope highlight.ts error). Runtime smoke test via agent-browser against the built binary: tree rendered 9 files/8 dirs, clicking App.tsx scrolled the diff (scrollTop 622) and flashed the section, .beads dir collapsed 9→8 files, panel collapsed to a 28px rail and re-expanded, zero console errors.\n\nAcceptance: 7/7 criteria met. Files: web/src/filetree.ts (new), web/src/filetree.test.ts (new), web/src/FileTreePanel.tsx (new), web/src/App.tsx, web/src/styles.css. NOTE: component file is FileTreePanel.tsx, not FileTree.tsx as the spec named it — on macOS's case-insensitive FS, FileTree.tsx collided with filetree.ts and broke both the vite build and tsc (import './FileTree' resolved to the pure module). The exported component is still named FileTree; only the filename differs. Not committed, per instructions — awaiting your review.","created_at":"2026-05-21T02:31:13Z"},{"id":9,"issue_id":"loupe-2sq.2","author":"coordinator","text":"Review APPROVED by worker-5","created_at":"2026-05-21T02:34:37Z"},{"id":10,"issue_id":"loupe-2sq.2","author":"coordinator","text":"Task completed","created_at":"2026-05-21T02:37:05Z"}]}
{"id":"loupe-2sq.3","title":"Context-aware comment anchoring to mitigate line drift","description":"Comments currently anchor on (path, line) only — store.Comment.BlobSHA exists but is never populated (the frontend CommentInput sends just {path,side,line,body}, and App.tsx renders comments by matching c.path===f.path \u0026\u0026 c.line===ln). When the underlying diff shifts (working-tree edits, or amend/rebase of a committed branch), a comment's stored line points at the wrong content or vanishes from view. This adds a content anchor so drifted comments relocate instead of silently misplacing.\n\nScope here is the MINIMAL v1; robust version noted as follow-on.\n\n## v1 (this issue)\n### Capture (cheap — most of it already exists)\n- store.go: add an 'anchor_text' column via the existing migrations mechanism (the ALTER TABLE … 'duplicate column name'-tolerant list). Trivial; pattern is already there.\n- web/src/api.ts: widen CommentInput with the captured line text. The content is already in hand at capture time — openNew(path, line: DiffLine) holds line.content; stash it in the editor target so saveEditor can send it.\n- handleAddComment already decodes into store.Comment, so the field flows through with no handler change once the struct has it.\n\n### Relocation (the real logic — pure + unit-tested)\n- A pure helper (e.g. web/src/anchor.ts): given a stored anchor (anchor_text + original line) and the freshly parsed FileDiff, return the best current line. Heuristic: verify the line at the stored number still matches anchor_text; else search outward ±N lines for an exact content match; else flag the comment orphaned.\n- Unit-test under the existing Vitest harness — cover the shifted-line, duplicate-line, and deleted-line cases.\n\n### Orphaned-comment UI\n- Today comments only render inline at a matched (path, line); an unrelocatable comment currently just disappears. Add a simple per-file collapsed 'outdated comments' section so orphans have a home.\n\n## Follow-on (NOT this issue — file separately if pursued)\n- Context window (context_before/after lines) for more robust matching when a line's text is duplicated.\n- Populate + use blob_sha as a fast path: file unchanged since the comment → trust the stored line as-is, skip relocation.\n- Properly tuned fuzzy matcher.\n\n## Notes\n- Independent of the working-tree feature (loupe-2sq.1): also fixes drift on amend/rebase of a committed branch. Natural companion, not a dependency.","acceptance_criteria":"An 'anchor_text' column captures the commented line's content at creation. On render, a comment whose stored line no longer matches relocates to the nearest line with matching content (searched within a ±N window); a comment that can't be relocated appears in a per-file 'outdated comments' section instead of vanishing. A pure relocation helper is covered by tests (shifted-line, duplicate-line, deleted-line cases) and 'make test' passes.","notes":"Effort estimate: ~0.5 day for this v1; ~1.5–2 days for the robust follow-on. Capture is nearly free (BlobSHA column + migration pattern already exist, frontend already holds line.content); the relocation function and orphaned-comment UI are the actual work.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-05-20T21:27:30.137392-04:00","created_by":"smore","updated_at":"2026-05-20T22:49:58.46365-04:00","closed_at":"2026-05-20T22:49:58.463653-04:00","dependencies":[{"issue_id":"loupe-2sq.3","depends_on_id":"loupe-2sq","type":"parent-child","created_at":"2026-05-20T21:27:30.140422-04:00","created_by":"smore"}],"comments":[{"id":11,"issue_id":"loupe-2sq.3","author":"coordinator","text":"Implementation complete: Implemented context-aware comment anchoring (v1) to mitigate line drift.\n\nBACKEND (internal/store/store.go): Added `anchor_text` column via the existing migration mechanism (ALTER TABLE + duplicate-column tolerance), plus to the CREATE TABLE schema. Added AnchorText field to Comment struct and threaded it through AddComment INSERT, Comments/CommentByID/MarkSubmitted SELECTs, and scanComments. handleAddComment needs no change (decodes into store.Comment) — verified by API round-trip test.\n\nFRONTEND CAPTURE (web/src/api.ts, App.tsx): Widened CommentInput and StoredComment with anchor_text. openNew now stashes the clicked DiffLine's content into the editor target; saveEditor sends it on create. Hydration maps anchor_text → anchorText so persisted comments re-anchor after reload.\n\nRELOCATION (NEW web/src/anchor.ts): Pure relocate(anchor, file, window) — keeps the comment if the stored line still matches the anchor text, else picks the nearest line within ±N (default 5) with exact matching content, else returns null (orphaned). Empty anchor (legacy rows) falls back to stored line. App.tsx resolves all comments via a useMemo into inline (by current line) vs orphaned buckets.\n\nORPHANED UI: Per-file collapsed \u003cdetails\u003e \"outdated comments\" section (styled in styles.css) so unrelocatable comments show (\"was line N\") with edit/delete instead of vanishing. Refactored editorRow into reusable editorForm.\n\nTESTS: NEW web/src/anchor.test.ts — 6 cases (stays, shifted-down, duplicate-line-nearest-wins, deleted/orphaned, outside-window, legacy-empty-anchor). `make test` passes: Go all green + Vitest 22/22.\n\nRUNTIME SMOKE TEST (built binary, port 7879, agent-browser): Selected working-tree mode against a 5-line scratch file; commented on line 3; (1) comment rendered inline; (2) inserted 2 lines above → Refresh → comment relocated to line 5 under the same content; (3) deleted the line → Refresh → comment moved to \"1 outdated comment · no longer match a line\" section showing \"was line 3\"; (4) restored line + full page reload → comment re-hydrated and re-anchored correctly. Console clean (no errors/warnings). Cleaned up scratch file, smoke comment, killed server.\n\nNo new tsc errors (pre-existing highlight.ts error untouched). No dead code (relocate/editorForm/DEFAULT_WINDOW all used in production). NOT committed — awaiting review approval.\n\nFiles: internal/store/store.go, web/src/api.ts, web/src/App.tsx, web/src/styles.css, web/src/anchor.ts (new), web/src/anchor.test.ts (new).","created_at":"2026-05-21T02:44:55Z"},{"id":12,"issue_id":"loupe-2sq.3","author":"coordinator","text":"Review APPROVED by worker-7","created_at":"2026-05-21T02:47:32Z"},{"id":13,"issue_id":"loupe-2sq.3","author":"coordinator","text":"Task completed","created_at":"2026-05-21T02:49:58Z"}]}
{"id":"loupe-2sq.4","title":"Fold working changes into the current-branch view (no separate selection)","description":"Refinement of loupe-2sq.1. When the selected branch IS the currently checked-out branch, the diff should automatically include uncommitted working changes — the user should not have to pick the separate '*working*' (Working tree) option to see their in-progress work against base.\n\n## Behavior\n- If branch == git.CurrentBranch(): show 'git diff \u003cmerge-base(base, HEAD)\u003e' (committed + uncommitted together) — i.e. route to the same DiffWorking path 2sq.1 adds.\n- If branch is any OTHER (non-checked-out) branch: keep the commit-to-commit diff (you can't show a working tree for a branch that isn't checked out).\n- The branch picker already defaults to the current branch (App.tsx useEffect on getBranches), so the default view becomes 'my current work including uncommitted changes' with no extra clicks.\n\n## Implementation\n- internal/server/server.go: diffFor already centralizes the dispatch (it currently routes the git.WorkingRef sentinel to DiffWorking). Extend it: also route to DiffWorking(base) when branch == s.git.CurrentBranch(). runReview already shares diffFor, so the agent prompt stays consistent with the live view.\n- No new frontend selection needed. Optionally drop or de-emphasize the explicit '*working*' option if this makes it redundant for the common case (see open question).\n\n## Open questions / decisions\n- Does this SUPERSEDE the explicit WorkingRef sentinel from 2sq.1? The sentinel is still useful for reviewing the working tree against an arbitrary base, or uncommitted edits while on the base branch itself — but folding working changes into the current-branch view may make the separate dropdown entry unnecessary for most users. Decide whether to keep the sentinel as an advanced option or remove it.\n- Consider a 'committed only' toggle, since with this change the current-branch view no longer shows the pure committed diff.\n\n## Caveats\n- Makes the current-branch view live (changes as you edit), so the anchor-drift concern in loupe-2sq.3 now applies to the default view, not just the opt-in working-tree view.","acceptance_criteria":"Selecting (or defaulting to) the currently checked-out branch shows a diff that includes uncommitted working changes against base, without choosing a separate option. Non-checked-out branches still show the committed-only diff. diffFor and runReview agree on the diff. Covered by a server test (current branch includes an uncommitted edit; a different branch does not). 'go test ./...' passes.","notes":"Discovered while planning 2sq.1; depends on its DiffWorking/diffFor machinery.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-05-20T22:26:34.976888-04:00","created_by":"smore","updated_at":"2026-05-20T23:04:36.934362-04:00","closed_at":"2026-05-20T23:04:36.934366-04:00","dependencies":[{"issue_id":"loupe-2sq.4","depends_on_id":"loupe-2sq","type":"parent-child","created_at":"2026-05-20T22:26:34.977644-04:00","created_by":"smore"},{"issue_id":"loupe-2sq.4","depends_on_id":"loupe-2sq.1","type":"discovered-from","created_at":"2026-05-20T22:26:35.169935-04:00","created_by":"smore"},{"issue_id":"loupe-2sq.4","depends_on_id":"loupe-2sq.1","type":"blocks","created_at":"2026-05-20T22:26:35.353342-04:00","created_by":"smore"}],"comments":[{"id":14,"issue_id":"loupe-2sq.4","author":"coordinator","text":"Implementation complete: Implemented loupe-2sq.4: folded working-tree changes into the current-branch view. EXTENDED internal/server/server.go diffFor() to route to git.DiffWorking(base) when branch == s.git.CurrentBranch() (in addition to the existing git.WorkingRef sentinel); guards against empty CurrentBranch (detached HEAD). Since runReview and handleDiff both call diffFor, the live view and agent prompt stay consistent automatically. CurrentBranch() already existed in internal/git/git.go (used by handleBranches), so no new git method was needed. Did NOT remove the *working* sentinel (kept as advanced option per spec) and did NOT add the optional committed-only toggle (out of scope).\n\nTESTS: Added TestDiffCurrentBranchFoldsWorkingTree (server_test.go) — current/checked-out branch (feature) includes both a committed change AND an uncommitted edit; a different non-checked-out branch (main) stays committed-only. Also updated the existing TestDiffWorkingRefRoutesToWorkingTree: its committed-only assertion previously used branch=main, which is now the current branch and would route to DiffWorking — switched it to a non-checked-out branch (other) so the assertion stays meaningful. go test ./... passes (all packages), go vet clean, full `make test` passes (Go + 24 frontend vitest tests).\n\nSMOKE TEST: built via `make build`, ran on 127.0.0.1:7879, drove via agent-browser. With the default (current) branch auto-selected — NOT the *working* sentinel — a temporary uncommitted edit (SMOKE_TEST_MARKER_2sq4 appended to AGENTS.md) appeared in the file-tree sidebar (AGENTS.md +2) and in the rendered diff. agent-browser console + errors both empty (no console/WebSocket errors). Reverted the temp edit, closed browser, killed server.\n\nACCEPTANCE: 5/5 — default/current branch shows uncommitted changes vs base with no extra clicks; non-checked-out branches still committed-only; diffFor and runReview agree; server test covers both cases; go test ./... passes.\n\nFiles changed: internal/server/server.go (diffFor), internal/server/server_test.go (1 new test + 1 updated test). internal/server/web_dist/index.html shows a regenerated JS asset hash from `make build` (the committed index.html references a gitignored/generated asset, so reverting would point at a missing file — kept the working rebuilt version). .beads/issues.jsonl was already modified at session start (bd tracking). NOT committed per coordinator instruction — awaiting review.","created_at":"2026-05-21T02:57:04Z"},{"id":15,"issue_id":"loupe-2sq.4","author":"coordinator","text":"Review APPROVED by worker-9","created_at":"2026-05-21T02:59:33Z"},{"id":16,"issue_id":"loupe-2sq.4","author":"coordinator","text":"Task completed","created_at":"2026-05-21T03:04:37Z"}]}
{"id":"loupe-5fv","title":"Phase 4: frontend dynamic prompt dropdown","description":"## Summary\nMake the Output dropdown dynamic: fetch the available prompts from the new `/api/prompts` endpoint, render an `\u003coption\u003e` per prompt, and preselect the server-provided global last selection. Removes the hardcoded `Mode` union. Depends on Phase 3 (the endpoint).\n\n## Files\n- Modify `web/src/api.ts`\n- Modify `web/src/App.tsx`\n\n## Implementation Details\n\n### api.ts\nAdd a type and fetch helper (route through the existing `json()` wrapper):\n```ts\nexport interface PromptInfo { id: string; name: string }\nexport async function listPrompts(): Promise\u003c{ prompts: PromptInfo[]; selected: string }\u003e {\n  return json(await fetch('/api/prompts'))\n}\n```\n`submitReview(reviewId, mode)` is unchanged: `mode` now carries the selected prompt id (a filename stem).\n\n### App.tsx\n- Replace `type Mode = 'beads' | 'document' | 'direct'` (line 19) and `const [mode, setMode] = useState\u003cMode\u003e('document')` (line 62) with:\n  ```ts\n  const [mode, setMode] = useState\u003cstring\u003e('document')\n  const [prompts, setPrompts] = useState\u003cPromptInfo[]\u003e([])\n  ```\n- Add a mount effect next to the `getBranches` effect (lines 75-81):\n  ```ts\n  useEffect(() =\u003e {\n    listPrompts().then(({ prompts, selected }) =\u003e {\n      setPrompts(prompts)\n      setMode(selected)\n    })\n  }, [])\n  ```\n- Replace the hardcoded `\u003coption\u003e` list (lines 509-511) with:\n  ```tsx\n  {prompts.map((p) =\u003e \u003coption key={p.id} value={p.id}\u003e{p.name}\u003c/option\u003e)}\n  ```\n  Keep the `\u003cselect value={mode} onChange={(e) =\u003e setMode(e.target.value)}\u003e` wrapper (lines 506-513); drop the `as Mode` cast (now a plain string).\n- Import `listPrompts` and `type PromptInfo` from `./api`.\n\n## Gotchas \u0026 Context\n- Frontend uses plain React hooks, no state library, and no `localStorage` (verified in research). Hydration is server-driven via the new endpoint; do NOT add `localStorage`.\n- The dropdown selection is local until submit; global persistence happens server-side on submit (Phase 3). This matches \"remember last selection\" = last submitted.\n- There is no frontend test harness in this repo; verify manually (see below) and note it in the PR description.\n\n## Acceptance Criteria\n- [ ] Dropdown lists the prompts returned by `/api/prompts` (three after a fresh seed), labeled by `name`.\n- [ ] Dropping a new `.md` into `.loupe/agent/prompts/` makes it appear in the dropdown after reload, with no code change.\n- [ ] The last-submitted prompt is preselected on a fresh page load (across branches).\n- [ ] `npm run build` (or the project's typecheck/build) passes with no `Mode`-type references remaining.\n- [ ] Manual verification performed and noted in the PR (dropdown contents, custom-file pickup, preselection).\n\n## References\n- Plan: .docs/plans/2026-05-20-configurable-agent-prompts.md (Phase 5)\n- Research: .docs/research/2026-05-20-configurable-agent-prompts.md\n- Depends on: loupe-eip (Phase 3)\n- Epic: loupe-rti","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-21T10:53:33.036916-04:00","created_by":"smore","updated_at":"2026-05-21T11:29:55.487666-04:00","closed_at":"2026-05-21T11:29:55.487666-04:00","close_reason":"Phase 4 complete: dynamic Output dropdown via GET /api/prompts; reviewed and APPROVED by worker-8","dependencies":[{"issue_id":"loupe-5fv","depends_on_id":"loupe-rti","type":"parent-child","created_at":"2026-05-21T10:53:40.01676-04:00","created_by":"smore"},{"issue_id":"loupe-5fv","depends_on_id":"loupe-eip","type":"blocks","created_at":"2026-05-21T10:53:40.591083-04:00","created_by":"smore"}],"comments":[{"id":26,"issue_id":"loupe-5fv","author":"coordinator","text":"Implementation complete: Phase 4 complete: Output dropdown is now dynamic, driven by GET /api/prompts.\n\nChanges (frontend only, per task scope):\n- web/src/api.ts: added `PromptInfo` interface and `listPrompts(): Promise\u003c{prompts; selected}\u003e`, routed through the existing `json()`/`fetch` wrapper matching getBranches. submitReview(reviewId, mode) unchanged.\n- web/src/App.tsx: removed `type Mode` union and `useState\u003cMode\u003e`; now `useState\u003cstring\u003e('document')` + `useState\u003cPromptInfo[]\u003e([])`. Added a mount effect (next to getBranches) calling listPrompts() to set prompts + preselect server-provided `selected`. Replaced hardcoded \u003coption\u003e list with `prompts.map(...)`; dropped the `as Mode` cast. Imported listPrompts + type PromptInfo.\n- internal/server/web_dist/index.html: rebuilt bundle reference (tracked build artifact; hashed assets gitignored).\n\nIMPORTANT DEVIATION FROM SPEC: The task spec specified `PromptInfo { id; name }` (lowercase), but the merged Phase 3 endpoint serializes prompts.Prompt{ID, Name} with NO json tags, so the API actually returns capitalized `ID`/`Name` (verified: curl /api/prompts -\u003e {\"prompts\":[{\"ID\":\"beads\",\"Name\":\"Beads\"},...],\"selected\":\"document\"}). Following the lowercase spec literally would have rendered blank/undefined option labels and keys. Since my mandate was frontend-only files, I aligned the TS interface and JSX to the real API shape (ID/Name). Alternative fix (out of my scope) would be adding `json:\"id\"`/`json:\"name\"` tags to internal/prompts/prompts.go for a more idiomatic lowercase API — flagging for reviewer to decide.\n\nVERIFICATION:\n1. Build/typecheck: `npm run build` (vite) passes clean. `npx tsc --noEmit` shows only one PRE-EXISTING unrelated error in highlight.ts (ignoreIllegal typo) — none in my files. grep confirms zero `Mode`-type references remain (`as Mode` / `type Mode` / `\u003cMode\u003e`).\n2. Runtime smoke test: built the loupe binary (web_dist is embedded via go:embed, so rebuilt binary), ran it on 127.0.0.1:8799, loaded with agent-browser. Results:\n   - Dropdown lists all three seeded prompts labeled by Name: \"Beads::beads | Direct::direct | Document::document\". ✅\n   - No console errors on load (window.__console_errors == []). ✅\n   - `document` preselected (matches server-provided selected). ✅\n   - Custom-file pickup: dropped zzcustom.md into .loupe/agent/prompts/, reloaded — \"Zzcustom::zzcustom\" appeared with no code change, still no console errors. ✅ (cleaned up the test file after.)\n3. Cleaned up: killed server, removed temp binary/shim. No frontend test harness in repo (vitest exists but no tests for this) — verified manually as noted; should be called out in the PR.\n\nAcceptance criteria: 5/5 met (dropdown contents, custom-file pickup, server-driven preselection, build passes with no Mode refs, manual verification performed). Note the ID/Name spec deviation above for review. Did NOT commit per instructions.","created_at":"2026-05-21T15:26:17Z"},{"id":27,"issue_id":"loupe-5fv","author":"coordinator","text":"Review APPROVED by worker-8","created_at":"2026-05-21T15:29:18Z"}]}
{"id":"loupe-8xb","title":"Phase 1: internal/prompts package (embed, seed, list, render)","description":"## Summary\nAdd a new `internal/prompts` package that owns on-disk prompt files: embedded defaults, seed-once initialization, listing, and `text/template` rendering. Wire seeding into `main.go` so `.loupe/agent/prompts/` is populated on first run. This is the foundation the server (Phase 3) reads from. It compiles and ships independently (the server still uses the old adapter until Phase 3).\n\n## Files\n- Create `internal/prompts/prompts.go`\n- Create `internal/prompts/defaults/beads.md`\n- Create `internal/prompts/defaults/document.md`\n- Create `internal/prompts/defaults/direct.md`\n- Create `internal/prompts/prompts_test.go`\n- Modify `cmd/loupe/main.go` (seed after the `.loupe` dir creation, around line 31)\n\n## Implementation Details\n- Embed defaults (the `defaults/` dir must sit beside `prompts.go`; `//go:embed` paths are relative to the source file):\n  ```go\n  //go:embed defaults/*.md\n  var defaultsFS embed.FS\n  ```\n- Loader bound to the prompts directory:\n  ```go\n  type Loader struct{ dir string }\n  func New(root string) *Loader {\n      return \u0026Loader{dir: filepath.Join(root, \".loupe\", \"agent\", \"prompts\")}\n  }\n  ```\n- Types:\n  ```go\n  type Prompt struct { ID, Name string } // ID = filename stem (e.g. \"document\"); Name = display label (title-case of stem)\n  type Vars   struct { ReviewID int64; Branch, Base string }\n  ```\n- `func (l *Loader) Seed() error`: if `l.dir` exists, return nil (seed-once, decision 5). Otherwise `os.MkdirAll(l.dir, 0o755)` and copy each embedded `defaults/*.md` into `l.dir/\u003cname\u003e` with `os.WriteFile(..., 0o644)`. Use `fs.ReadDir`/`fs.ReadFile` over `defaultsFS`.\n- `func (l *Loader) List() ([]Prompt, error)`: read `l.dir`, keep `.md` files, return sorted by ID. If `l.dir` is missing, return an empty slice and nil error (defensive).\n- `func (l *Loader) Render(id string, v Vars) (string, error)`: read `\u003cdir\u003e/\u003cid\u003e.md`; if missing, return `fmt.Errorf(\"prompt %q not found\", id)`. Parse with `text/template`, execute against `v`, return the rendered string with trailing whitespace trimmed.\n- Default file contents (carried over verbatim from `adapter.instruction`, `%d` -\u003e `{{.ReviewID}}`):\n  - `beads.md`: \"Using the `bd` CLI, create one epic issue for this review and a child issue for each cluster of related comments, wiring up parent/child dependencies. Do not modify code: only create issues.\"\n  - `document.md`: \"Write a markdown implementation plan that addresses every comment to `.loupe/plans/review-{{.ReviewID}}.md`. Research the codebase as needed. Do not modify any other files.\"\n  - `direct.md`: \"Address each comment by editing the working tree directly. After each change, briefly state which comment it resolves.\"\n- In `main.go`, after the `.loupe` `MkdirAll` block (lines 28-31), add (import `github.com/calebjdinsmore/loupe/internal/prompts`):\n  ```go\n  if err := prompts.New(root).Seed(); err != nil {\n      log.Fatal(err)\n  }\n  ```\n\n## Gotchas \u0026 Context\n- Module path is `github.com/calebjdinsmore/loupe` (see imports in `main.go:16-19`).\n- Repo root is resolved via `gitRoot()` (`main.go:23,61-67`) and is already on hand where `.loupe` is created.\n- Avoid emdashes in default prompt copy (use colons), matching repo copy style.\n- The loader is constructed again for the server in Phase 3; constructing twice is cheap. Implementer may instead construct once and pass to both `Seed()` and `server.New` — either is fine.\n\n## Acceptance Criteria\n- [ ] `Seed()` into an empty root creates `beads.md`, `document.md`, `direct.md` with the contents above.\n- [ ] `Seed()` is a no-op when the dir already exists (edit a default, re-seed, assert unchanged; no defaults re-added).\n- [ ] `List()` returns the three seeded prompts sorted by ID, ignoring non-`.md` files.\n- [ ] `Render(\"document\", Vars{ReviewID: 42})` contains `review-42.md`.\n- [ ] `Render(\"missing\", ...)` returns an error.\n- [ ] `main.go` seeds on startup; running `loupe` in a fresh repo creates `.loupe/agent/prompts/` with the three files.\n- [ ] `go test ./internal/prompts/...` passes (tests use `t.TempDir()` as root).\n\n## References\n- Plan: .docs/plans/2026-05-20-configurable-agent-prompts.md (Phase 1)\n- Research: .docs/research/2026-05-20-configurable-agent-prompts.md\n- Epic: loupe-rti","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-21T10:52:41.646954-04:00","created_by":"smore","updated_at":"2026-05-21T11:07:04.752042-04:00","closed_at":"2026-05-21T11:07:04.752046-04:00","dependencies":[{"issue_id":"loupe-8xb","depends_on_id":"loupe-rti","type":"parent-child","created_at":"2026-05-21T10:53:39.440786-04:00","created_by":"smore"}],"comments":[{"id":17,"issue_id":"loupe-8xb","author":"coordinator","text":"Implementation complete: Implemented Phase 1: internal/prompts package (loupe-8xb).\n\nFiles created:\n- internal/prompts/prompts.go — embedded defaults (//go:embed defaults/*.md), Loader{dir} via New(root) bound to \u003croot\u003e/.loupe/agent/prompts, Prompt{ID,Name} and Vars{ReviewID,Branch,Base} types, and Seed()/List()/Render() methods per spec.\n- internal/prompts/defaults/{beads,document,direct}.md — verbatim task text with colons (no emdashes); document.md uses {{.ReviewID}}.\n- internal/prompts/prompts_test.go — 6 tests using t.TempDir() as root.\nFiles modified:\n- cmd/loupe/main.go — added prompts import and `prompts.New(root).Seed()` right after the .loupe MkdirAll block.\n\nAcceptance: 7/7 criteria met with evidence.\n1. Seed into empty root creates beads.md/document.md/direct.md with correct contents — TestSeedCreatesDefaults + startup smoke test.\n2. Seed is no-op when dir exists (edited+removed a default, re-seed left both unchanged) — TestSeedIsNoOpWhenDirExists.\n3. List returns 3 prompts sorted by ID, ignoring non-.md (notes.txt) — TestList.\n4. Render(\"document\", Vars{ReviewID:42}) contains \"review-42.md\" — TestRender (also asserts trailing whitespace trimmed).\n5. Render(\"missing\",...) returns error — TestRenderMissing.\n6. main.go seeds on startup — built binary, ran in fresh git repo, .loupe/agent/prompts/ created with all three files (verified contents).\n7. List on missing dir returns empty slice + nil error — TestListMissingDir.\n\nTests: 6/6 passing (go test ./internal/prompts/...). Gates: `go build ./...`, `go vet ./internal/prompts/...`, and package tests all pass.\n\nNotes: List/Render are unused in production this phase by design — they are the public API Phase 3 (loupe-eip, server wiring) consumes; the task specifies them as Phase 1 deliverables. Only prompts.New().Seed() is wired into production now (verified via grep). No commit made — awaiting your review approval.","created_at":"2026-05-21T15:03:10Z"},{"id":18,"issue_id":"loupe-8xb","author":"coordinator","text":"Review APPROVED by worker-2","created_at":"2026-05-21T15:05:16Z"},{"id":19,"issue_id":"loupe-8xb","author":"coordinator","text":"Task completed","created_at":"2026-05-21T15:07:04Z"}]}
{"id":"loupe-cpu","title":"Build loupe as a VS Code extension","description":"Pivot from the Go web app to a native VS Code extension. The scaffold (this commit) covers the core loop: Review Branch -\u003e native diff editor over git: URIs -\u003e line comments via the Comments API -\u003e Submit Review -\u003e mode pick (document/beads/direct) -\u003e spawn claude headless and stream the session into a webview. This epic tracks the work to take that scaffold to a publishable v1.","acceptance_criteria":"Extension installs from a .vsix; full review-\u003ecomment-\u003esubmit-\u003eagent loop works in the Extension Development Host; child issues completed.","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-05-20T16:36:07.47288-04:00","created_by":"smore","updated_at":"2026-05-20T17:09:59.611126-04:00","closed_at":"2026-05-20T17:09:59.611126-04:00","close_reason":"Reverted: VS Code extension pivot abandoned; back on the web app (parked on branch vscode-extension-attempt)."}
{"id":"loupe-cpu.1","title":"Persist review state (comments + resolve status) across reloads","description":"Comment threads and the active review session live only in memory (src/extension.ts). Reloading the window loses them. Persist threads (uri, range, body, resolved) and the session (base/branch/mergeBase) to context.workspaceState (or a .loupe file) and rehydrate the comment controller on activate.","acceptance_criteria":"After reloading the window mid-review, comment threads reappear at the right lines with their resolved state intact.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-05-20T16:36:19.709959-04:00","created_by":"smore","updated_at":"2026-05-20T17:09:59.616373-04:00","closed_at":"2026-05-20T17:09:59.616373-04:00","close_reason":"Reverted: VS Code extension pivot abandoned; back on the web app (parked on branch vscode-extension-attempt).","dependencies":[{"issue_id":"loupe-cpu.1","depends_on_id":"loupe-cpu","type":"parent-child","created_at":"2026-05-20T16:36:19.712651-04:00","created_by":"smore"}]}
{"id":"loupe-cpu.2","title":"Restrict commenting ranges to changed lines only","description":"commentingRangeProvider currently allows comments on every line of a diff document. Parse the per-file diff (or use the Change hunks) to expose only added/changed line ranges, matching how PR review tools gate comments.","acceptance_criteria":"The + commenting affordance only appears on lines that are part of the diff.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-05-20T16:36:20.005819-04:00","created_by":"smore","updated_at":"2026-05-20T17:09:59.617982-04:00","closed_at":"2026-05-20T17:09:59.617982-04:00","close_reason":"Reverted: VS Code extension pivot abandoned; back on the web app (parked on branch vscode-extension-attempt).","dependencies":[{"issue_id":"loupe-cpu.2","depends_on_id":"loupe-cpu","type":"parent-child","created_at":"2026-05-20T16:36:20.006544-04:00","created_by":"smore"}]}
{"id":"loupe-cpu.3","title":"Agent panel: running/done status and interactive follow-up via --resume","description":"The webview (src/panel.ts) only appends events. Add a running/done indicator, capture the session_id from the system event, and add an input box that continues the session with 'claude -p --resume \u003cid\u003e --input-format stream-json' so the user can reply to the agent in-panel.","acceptance_criteria":"Panel shows when the agent is working vs done; user can send a follow-up message that the same agent session answers.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-05-20T16:36:20.297986-04:00","created_by":"smore","updated_at":"2026-05-20T17:09:59.619338-04:00","closed_at":"2026-05-20T17:09:59.619338-04:00","close_reason":"Reverted: VS Code extension pivot abandoned; back on the web app (parked on branch vscode-extension-attempt).","dependencies":[{"issue_id":"loupe-cpu.3","depends_on_id":"loupe-cpu","type":"parent-child","created_at":"2026-05-20T16:36:20.298686-04:00","created_by":"smore"}]}
{"id":"loupe-cpu.4","title":"Publish prep: publisher id, icon, LICENSE, marketplace metadata","description":"Before publishing: set a real publisher (currently 'loupe'), add an icon and LICENSE, fill repository/bugs/homepage in package.json, and verify 'vsce package' is warning-free without --allow-missing-repository/--skip-license.","acceptance_criteria":"vsce package produces a clean .vsix with no missing-metadata warnings.","status":"closed","priority":3,"issue_type":"task","created_at":"2026-05-20T16:36:20.594564-04:00","created_by":"smore","updated_at":"2026-05-20T17:09:59.620705-04:00","closed_at":"2026-05-20T17:09:59.620705-04:00","close_reason":"Reverted: VS Code extension pivot abandoned; back on the web app (parked on branch vscode-extension-attempt).","dependencies":[{"issue_id":"loupe-cpu.4","depends_on_id":"loupe-cpu","type":"parent-child","created_at":"2026-05-20T16:36:20.595288-04:00","created_by":"smore"}]}
{"id":"loupe-e0i","title":"Phase 2: settings key/value store","description":"## Summary\nAdd a generic `settings` key/value table to the SQLite store, with `Setting`/`SetSetting` accessors. Phase 3 uses key `last_prompt` to persist the global last-submitted prompt selection. Compiles and ships independently.\n\n## Files\n- Modify `internal/store/store.go`\n- Modify `internal/store/store_test.go`\n\n## Implementation Details\n- Add to the `schema` const, after the `comments` table (around line 63):\n  ```sql\n  CREATE TABLE IF NOT EXISTS settings (\n    key   TEXT PRIMARY KEY,\n    value TEXT NOT NULL\n  );\n  ```\n  `CREATE TABLE IF NOT EXISTS` runs on every `Open` (`store.go:82`) and is idempotent, so it also creates the table on pre-existing databases. Do NOT add an entry to the `migrations` slice (that slice is only for `ALTER TABLE` column adds, lines 68-72).\n- Add methods:\n  ```go\n  // Setting returns the stored value for key, or \"\" if unset.\n  func (s *Store) Setting(key string) (string, error) {\n      var v string\n      err := s.db.QueryRow(`SELECT value FROM settings WHERE key = ?`, key).Scan(\u0026v)\n      if err == sql.ErrNoRows {\n          return \"\", nil\n      }\n      return v, err\n  }\n  // SetSetting upserts a settings value.\n  func (s *Store) SetSetting(key, value string) error {\n      _, err := s.db.Exec(\n          `INSERT INTO settings (key, value) VALUES (?, ?)\n           ON CONFLICT(key) DO UPDATE SET value = excluded.value`, key, value)\n      return err\n  }\n  ```\n\n## Gotchas \u0026 Context\n- `database/sql` is already imported (used in `EnsureReview`, `store.go:113` references `sql.ErrNoRows`).\n- Single connection (`SetMaxOpenConns(1)`, `store.go:81`) means no concurrency concern.\n- Follow the existing test setup pattern in `store_test.go` (open a store on a temp DB path).\n\n## Acceptance Criteria\n- [ ] `Setting` on an unset key returns `\"\"`, nil.\n- [ ] `SetSetting` then `Setting` round-trips a value.\n- [ ] A second `SetSetting` on the same key overwrites (upsert), no error.\n- [ ] `go test ./internal/store/...` passes.\n\n## References\n- Plan: .docs/plans/2026-05-20-configurable-agent-prompts.md (Phase 3)\n- Research: .docs/research/2026-05-20-configurable-agent-prompts.md\n- Epic: loupe-rti","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-21T10:52:55.518448-04:00","created_by":"smore","updated_at":"2026-05-21T11:11:23.466033-04:00","closed_at":"2026-05-21T11:11:23.466036-04:00","dependencies":[{"issue_id":"loupe-e0i","depends_on_id":"loupe-rti","type":"parent-child","created_at":"2026-05-21T10:53:39.636564-04:00","created_by":"smore"}],"comments":[{"id":20,"issue_id":"loupe-e0i","author":"coordinator","text":"Implementation complete: Phase 2 (loupe-e0i): Added a generic settings key/value table to the SQLite store. Changes: internal/store/store.go — added `CREATE TABLE IF NOT EXISTS settings (key TEXT PRIMARY KEY, value TEXT NOT NULL)` to the schema const (NOT the migrations slice, since CREATE TABLE IF NOT EXISTS is idempotent on every Open); added Setting(key) (string, error) returning \"\"/nil for unset keys via sql.ErrNoRows handling, and SetSetting(key, value) error using INSERT ... ON CONFLICT(key) DO UPDATE SET value = excluded.value upsert. internal/store/store_test.go — added TestSettingsRoundTrip covering unset key returns \"\"/nil, SetSetting→Setting round-trip, and upsert overwrite without error. Tests: 7/7 passing (go test ./internal/store/...). Acceptance: 4/4 criteria met. go build ./..., go vet ./internal/store/..., and go test ./internal/store/... all pass. Not committed pending review.","created_at":"2026-05-21T15:08:20Z"},{"id":21,"issue_id":"loupe-e0i","author":"coordinator","text":"Review APPROVED by worker-4","created_at":"2026-05-21T15:10:08Z"},{"id":22,"issue_id":"loupe-e0i","author":"coordinator","text":"Task completed","created_at":"2026-05-21T15:11:23Z"}]}
{"id":"loupe-eip","title":"Phase 3: adapter refactor + server wiring (single commit)","description":"## Summary\nReplace the hardcoded `Mode` enum with prompt-file-driven runs and expose the new prompt API. This merges plan Phases 2 and 4 into ONE issue because they are a hard one-commit dependency: dropping the `adapter.Mode` enum / `AllowedTools` / `instruction` breaks the build until `server.runReview` is updated to call the new signatures. They must land in a single commit. Depends on Phase 1 (prompts loader) and Phase 2 (settings store).\n\n## Files\n- Modify `internal/adapter/adapter.go`\n- Modify `internal/adapter/adapter_test.go`\n- Modify `internal/server/server.go`\n- Modify `internal/server/server_test.go`\n- Modify `cmd/loupe/main.go` (pass loader into `server.New`)\n\n## Implementation Details\n\n### Adapter (`adapter.go`)\n- Delete `Mode`, the `ModeBeads/ModeDocument/ModeDirect` constants, `AllowedTools`, and `instruction` (lines 13-51).\n- Add a single permissive allowlist:\n  ```go\n  // DefaultTools is the permissive allowlist used for every prompt so headless\n  // runs do not stall on permission prompts.\n  func DefaultTools() []string {\n      return []string{\"Edit\", \"Write\", \"Read\", \"Grep\", \"Glob\", \"Bash\"}\n  }\n  ```\n- Change `BuildPrompt` to take the rendered task text instead of computing it:\n  ```go\n  func BuildPrompt(r store.Review, comments []store.Comment, diff, task string) string\n  ```\n  Replace the final `b.WriteString(instruction(Mode(r.Mode), r.ID))` (line 74) with `b.WriteString(task)`. Everything above (intro, `## Reviewer comments`, `## Diff under review`, `## Your task` header) is unchanged.\n- Update the package doc comment (lines 1-2) to drop the \"beads, document, or direct\" enumeration.\n\n### Server (`server.go`)\n- Add a `prompts *prompts.Loader` field to `Server` (struct lines 21-29) and a parameter to `New`:\n  ```go\n  func New(g *git.Service, st *store.Store, r *agent.Runner, p *prompts.Loader) *Server\n  ```\n- Register a list route alongside `handleBranches` (in `New`, around line 34): `m.Get(\"/api/prompts\", s.handlePrompts)`.\n- `handlePrompts`:\n  ```go\n  func (s *Server) handlePrompts(w http.ResponseWriter, _ *http.Request) {\n      list, err := s.prompts.List()\n      if err != nil { http.Error(w, err.Error(), http.StatusInternalServerError); return }\n      selected, _ := s.store.Setting(\"last_prompt\")\n      if !containsID(list, selected) {\n          selected = \"document\"\n          if !containsID(list, selected) \u0026\u0026 len(list) \u003e 0 { selected = list[0].ID }\n      }\n      writeJSON(w, map[string]any{\"prompts\": list, \"selected\": selected})\n  }\n  ```\n  Add a tiny local `containsID(list []prompts.Prompt, id string) bool` helper.\n- `handleEnsureReview` default (lines 154-157): replace `string(adapter.ModeDocument)` with the literal `\"document\"`.\n- `handleSubmitReview` (lines 268-273): after `SetMode`, also persist the global selection:\n  ```go\n  if req.Mode != \"\" {\n      if err := s.store.SetMode(id, req.Mode); err != nil { /* 500 */ return }\n      if err := s.store.SetSetting(\"last_prompt\", req.Mode); err != nil { /* 500 */ return }\n  }\n  ```\n- `runReview` (lines 296-302): render the selected prompt and use the single toolset. Render errors must be broadcast (not silently dropped) so the UI surfaces them instead of the agent getting an empty task:\n  ```go\n  task, err := s.prompts.Render(rev.Mode, prompts.Vars{ReviewID: rev.ID, Branch: rev.Branch, Base: rev.Base})\n  if err != nil {\n      h.broadcast(agent.Event{Type: agent.EventError, Text: err.Error()})\n      _ = s.store.SetStatus(id, \"error\")\n      return\n  }\n  prompt := adapter.BuildPrompt(rev, batch, diff, task)\n  tools := adapter.DefaultTools()\n  ```\n  Keep the `adapter` import (still used for `BuildPrompt`/`DefaultTools`).\n\n### main.go\n- Update `server.New(...)` call (line 39) to `server.New(git.New(root), st, agent.New(root), prompts.New(root))`.\n\n## Gotchas \u0026 Context\n- After deleting the `Mode` enum the package will not compile against the old `server.go:298-299` call, which is exactly why adapter + server must be one commit.\n- `rev.Mode` is now the selection identifier (filename stem). Legacy DB rows hold `beads`/`document`/`direct`, which match the seeded filenames (decision 6), so they resolve without migration.\n- `runReview` runs in a goroutine; it already broadcasts errors via `h.broadcast(agent.Event{Type: agent.EventError, ...})` (see lines 293, 304) and sets status `error` — follow that pattern for the render failure.\n- `server_test.go` already does filesystem setup (`os.WriteFile`); follow its temp-root pattern and call `prompts.New(root).Seed()` in setup so `List`/`Render` have files.\n- Only callers of `BuildPrompt`/`AllowedTools` are `server.go:298-299` and `adapter_test.go:16,24`.\n\n## Acceptance Criteria\n- [ ] `adapter.Mode`, `AllowedTools`, and `instruction` are gone; no dead references remain.\n- [ ] `adapter_test.go` updated to pass a `task` arg and drop `Mode: string(ModeDocument)`; asserts the working-ref vs branch header still differs and the task text appears under `## Your task`.\n- [ ] `GET /api/prompts` after seeding returns the three prompts and `selected: \"document\"` by default.\n- [ ] After a submit with `mode: \"direct\"`, `GET /api/prompts` returns `selected: \"direct\"`.\n- [ ] `selected` falls back to an existing prompt when `last_prompt` points at a non-existent id.\n- [ ] A submit referencing a deleted/renamed prompt broadcasts an error event and sets status `error` (no run with empty task).\n- [ ] Submitting still produces a prompt containing the rendered task (e.g. the document plan path `review-\u003cid\u003e.md`).\n- [ ] The whole module builds: `go build ./...` and `go test ./...` pass.\n\n## References\n- Plan: .docs/plans/2026-05-20-configurable-agent-prompts.md (Phases 2 + 4, merged per the single-commit rule)\n- Research: .docs/research/2026-05-20-configurable-agent-prompts.md\n- Depends on: loupe-8xb (Phase 1), loupe-e0i (Phase 2)\n- Epic: loupe-rti","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-21T10:53:19.93733-04:00","created_by":"smore","updated_at":"2026-05-21T11:22:15.374684-04:00","closed_at":"2026-05-21T11:22:15.374687-04:00","close_reason":"Phase 3 implemented and approved: adapter.Mode enum replaced with prompt-file-driven runs, /api/prompts exposed","dependencies":[{"issue_id":"loupe-eip","depends_on_id":"loupe-rti","type":"parent-child","created_at":"2026-05-21T10:53:39.826215-04:00","created_by":"smore"},{"issue_id":"loupe-eip","depends_on_id":"loupe-8xb","type":"blocks","created_at":"2026-05-21T10:53:40.209675-04:00","created_by":"smore"},{"issue_id":"loupe-eip","depends_on_id":"loupe-e0i","type":"blocks","created_at":"2026-05-21T10:53:40.403875-04:00","created_by":"smore"}],"comments":[{"id":23,"issue_id":"loupe-eip","author":"coordinator","text":"Implementation complete: Phase 3 (loupe-eip): replaced hardcoded adapter.Mode enum with prompt-file-driven runs and exposed the prompt API, all in one commit-ready changeset.\n\nADAPTER (adapter.go): Deleted Mode, ModeBeads/ModeDocument/ModeDirect, AllowedTools, and instruction. Added DefaultTools() returning {Edit,Write,Read,Grep,Glob,Bash}. Changed BuildPrompt to func(r store.Review, comments []store.Comment, diff, task string) string — final write is now b.WriteString(task); header/intro/diff sections unchanged. Updated package doc comment.\n\nSERVER (server.go): Added prompts *prompts.Loader field; New now takes p *prompts.Loader. Registered GET /api/prompts -\u003e handlePrompts (lists via prompts.List(), selected from store.Setting(\"last_prompt\") with fallback to \"document\" then list[0].ID via a local containsID helper). handleEnsureReview default now literal \"document\". handleSubmitReview persists store.SetSetting(\"last_prompt\", req.Mode) after SetMode (with 500 handling). runReview renders the selected prompt via prompts.Render(rev.Mode, ...) and broadcasts an EventError + sets status \"error\" on render failure before building the prompt; uses adapter.DefaultTools().\n\nmain.go: server.New now passed prompts.New(root).\n\nTESTS: adapter_test.go updated to pass task arg, drop Mode, and assert task text under \"## Your task\". server_test.go: newTestServer seeds a prompts loader; added 4 tests (defaults endpoint returns 3 prompts + selected document; selection persists on submit; fallback when last_prompt is stale; missing-prompt submit broadcasts error event + sets status error without spawning the agent).\n\nVERIFICATION: go build ./..., go vet ./..., go test ./... all pass. Runtime smoke test: built binary, ran in a fresh temp git repo, curl localhost:8765/api/prompts returned {\"prompts\":[beads,direct,document],\"selected\":\"document\"} and seeded the three .md files; process killed and temp dir cleaned. grep confirms no dead references to Mode/AllowedTools/instruction remain. Acceptance: 8/8 criteria met. Files changed: internal/adapter/adapter.go, internal/adapter/adapter_test.go, internal/server/server.go, internal/server/server_test.go, cmd/loupe/main.go. Not committed — awaiting your review.","created_at":"2026-05-21T15:16:03Z"},{"id":24,"issue_id":"loupe-eip","author":"coordinator","text":"Review APPROVED by worker-6","created_at":"2026-05-21T15:20:41Z"},{"id":25,"issue_id":"loupe-eip","author":"coordinator","text":"Task completed","created_at":"2026-05-21T15:22:15Z"}]}
{"id":"loupe-evg","title":"Harden review-persistence API: r.ok checks, tx for MarkSubmitted, id parse errors","description":"Follow-ups from the loupe-1mf.1 persistence work (commits 81e280f, fd3bea1), intentionally kept out of that approved diff. The repo currently has ZERO tests, so this ticket also bootstraps a test suite — the fixes below should land with coverage.\n\nStack: Go 1.23 backend (internal/), React+Vite frontend (web/), single binary via `make build`. Manual validation: drive the running app with `agent-browser` against /tmp/loupe-test (a repo with `main` vs `feat/multiply`).\n\n## 1. Frontend: check r.ok in all fetch helpers\nFile: web/src/api.ts (helpers ~lines 38–93). Each does `await fetch(...)` then reads the body without checking status. Concrete failures:\n- submitReview swallows a 400 \"no pending comments to submit\"; the UI proceeds as if it worked.\n- ensureReview returns undefined on error, which then builds a WebSocket URL `/api/reviews/undefined/ws`.\nFix: a shared guard, e.g.\n    async function json\u003cT\u003e(r: Response): Promise\u003cT\u003e {\n      if (!r.ok) throw new Error(`${r.status} ${await r.text()}`)\n      return r.json() as Promise\u003cT\u003e\n    }\nRoute every helper through it (plus a no-body variant for the 204 cases: updateComment, deleteComment, submitReview). Surface thrown errors in the submit/agent flow in web/src/App.tsx instead of failing silently.\nAC: every api.ts helper rejects on non-2xx with the response text; a 400 from submit shows a visible error and does NOT open a WebSocket; no request is ever made to /api/reviews/undefined/...\n\n## 2. Backend: transactional MarkSubmitted\nFile: internal/store/store.go, MarkSubmitted. It runs pendingComments() (SELECT) then a separate UPDATE. A comment inserted between the two gets flagged submitted=1, collapsed=1 by the UPDATE but is absent from the returned batch — so it's marked submitted yet never sent to the agent (silently dropped).\nFix: run SELECT + UPDATE in one *sql.Tx (BEGIN IMMEDIATE), or switch to a single `UPDATE comments SET submitted=1, collapsed=1 WHERE review_id=? AND submitted=0 RETURNING \u003call cols\u003e` (SQLite \u003e=3.35; modernc.org/sqlite supports RETURNING) so the flagged rows and the returned batch are provably identical.\nAC: the comments returned by MarkSubmitted are exactly the rows it flagged; a regression test covering the insert-during-submit interleaving passes.\n\n## 3. Backend: reject non-numeric path ids\nFile: internal/server/server.go — six sites of `id, _ := strconv.ParseInt(chi.URLParam(r, \"id\"), 10, 64)` (~lines 137, 147, 171, 199, 214, 270). The discarded error makes a bad id become 0 and hit the store as a silent miss. Add a helper:\n    func pathID(w http.ResponseWriter, r *http.Request) (int64, bool) {\n      id, err := strconv.ParseInt(chi.URLParam(r, \"id\"), 10, 64)\n      if err != nil { http.Error(w, \"invalid id\", http.StatusBadRequest); return 0, false }\n      return id, true\n    }\nUse it at all six sites (early-return on !ok).\nAC: GET /api/reviews/abc/comments (and the other id routes) returns 400, not 200/500-with-id-0.\n\n## 4. Decide on the unused GET /api/reviews/{id}/comments\nRegistered (handleListComments) but has no SPA caller — listReviews already embeds comments. Either remove the route+handler, or keep it with a one-line comment marking it intentional API surface.\nAC: the route is either gone or documented as deliberate.\n\n## Initialize a test suite\nNo tests exist. Bootstrap one so the fixes above land verified and future work has a harness. Go is the priority; frontend tests are a stretch goal.\n\n### Go (primary)\n`go test ./...` works with the stdlib — no deps to add. Add a Makefile target:\n    test:\n    \tgo test ./...\n    \tcd web \u0026\u0026 npm test --silent --if-present\nTests to write (table-driven where natural):\n- internal/store/store_test.go — open a throwaway DB via Open(filepath.Join(t.TempDir(), \"t.db\")) (modernc sqlite is cgo-free, runs anywhere). Cover: EnsureReview get-or-create idempotency; AddComment returns a usable id; MarkSubmitted returns exactly the flagged rows (the item-2 regression); UpdateComment / DeleteComment / Comments / Reviews filtering. Highest-value coverage — it's the persistence layer the feature rests on.\n- internal/server/server_test.go — build the router with server.New(...) and exercise it via net/http/httptest (httptest.NewRecorder + srv.ServeHTTP). Cover comment CRUD + reviews endpoints and the item-3 400 cases. The submit endpoint spawns claude, so test only its pre-spawn behavior (empty-pending -\u003e 400, which returns before `go runReview`). To test submit's success path without a real agent, extract the agent runner behind a small interface and inject a no-op — note as an OPTIONAL refactor, not required here.\n- internal/git/git_test.go (optional) — create a temp repo with git init + two branches; assert Branches() / MergeBase() / Diff(). Integration-flavored; fine to skip.\n\n### Frontend (stretch)\nNo runner configured. Add Vitest (npm i -D vitest), a \"test\": \"vitest run\" script, and:\n- web/src/diff.test.ts — parseUnifiedDiff is pure and the easiest high-value target (feed a known git diff, assert files/hunks/line kinds + line numbers).\n- The duplicate-`result` dedup logic (loupe-1mf.2) currently lives inline in App.tsx's ws.onmessage. Extract it into a pure helper (e.g. web/src/events.ts) and unit-test it — covers the bugfix and removes DOM coupling. React component tests (Testing Library) are out of scope.\n\n### CI (optional)\nA minimal .github/workflows/ci.yml running `go test ./...` + `npm ci \u0026\u0026 npm test` would lock this in, but isn't required to close the ticket.\n","acceptance_criteria":"All four items fixed: (1) every web/src/api.ts helper rejects on non-2xx and the UI surfaces it (never requests /api/reviews/undefined/...); (2) MarkSubmitted is atomic — the returned batch equals exactly the rows it flagged, with a regression test for the insert-during-submit interleaving; (3) all six path-id routes in server.go return 400 on a non-numeric id; (4) the unused GET /api/reviews/{id}/comments route is removed or documented as intentional. A Go test suite exists and 'go test ./...' passes, including store_test.go (EnsureReview idempotency + the MarkSubmitted regression) and server_test.go (the id-400 cases), wired into a 'make test' target. Previously-approved persistence still works (spot-checked via agent-browser).","status":"closed","priority":3,"issue_type":"chore","created_at":"2026-05-20T16:21:56.116439-04:00","created_by":"smore","updated_at":"2026-05-20T20:19:06.009975-04:00","closed_at":"2026-05-20T20:19:06.009975-04:00","close_reason":"Closed"}
{"id":"loupe-rti","title":"Configurable agent prompts via .loupe/agent/prompts","description":"Replace the hardcoded three-option `Mode` enum (beads / document / direct) with on-disk, user-editable prompt files under `.loupe/agent/prompts`. On first run the tool seeds three default `.md` files matching today's behavior; users can add their own. The app lists every prompt in the Output dropdown, runs the selected one (rendered through `text/template`), and remembers the last-submitted selection globally so it is preselected next time.\n\nPlan: .docs/plans/2026-05-20-configurable-agent-prompts.md\nResearch: .docs/research/2026-05-20-configurable-agent-prompts.md\n\nDecisions locked:\n1. Prompt files hold only the task text (the body under `## Your task`); the shared framing stays in `adapter.BuildPrompt`.\n2. Single permissive tool allowlist for all prompts: Edit, Write, Read, Grep, Glob, Bash.\n3. Prompt files support `text/template` placeholders: {{.ReviewID}}, {{.Branch}}, {{.Base}}.\n4. Last selection is one global setting (`settings` table, key `last_prompt`), updated on submit and read on load.\n5. Seed once: only when `.loupe/agent/prompts/` is absent.\n6. Default filenames match old mode values (beads.md, document.md, direct.md) for backward compat.\n7. Lazy seed in main.go on every startup (no-op when the dir exists).","status":"open","priority":2,"issue_type":"epic","created_at":"2026-05-21T10:52:22.675912-04:00","created_by":"smore","updated_at":"2026-05-21T10:52:22.675912-04:00"}
